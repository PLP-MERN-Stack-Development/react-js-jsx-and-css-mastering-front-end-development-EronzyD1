import { NavLink, Link } from 'react-router-dom';
import { useTheme } from '../context/ThemeContext';
export default function Header(){ const { theme, setTheme } = useTheme(); const link = ({isActive}) => `text-sm ${isActive ? 'font-semibold underline' : 'opacity-80 hover:opacity-100'}`; return (<header className='border-b bg-white/70 backdrop-blur dark:bg-gray-800/60'><div className='mx-auto flex max-w-6xl items-center justify-between px-4 py-3'><Link to='/' className='text-xl font-semibold'>🌍 Country Weather Explorer</Link><nav className='flex items-center gap-4'><NavLink to='/' className={link}>Home</NavLink><NavLink to='/tasks' className={link}>Tasks</NavLink><NavLink to='/posts' className={link}>Posts</NavLink><select aria-label='Theme' value={theme} onChange={e=>setTheme(e.target.value)} className='rounded-md border px-2 py-1 text-sm dark:bg-gray-700'><option value='system'>System</option><option value='light'>Light</option><option value='dark'>Dark</option></select></nav></div></header>); }